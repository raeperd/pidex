# Pidex v0.0.1: Desktop Pi workflow

The implementation plan is maintained in the [GitHub milestone description](https://github.com/raeperd/pidex/milestone/1). Update that description with `gh` CLI; this file preserves existing links and is not a separate spec.
