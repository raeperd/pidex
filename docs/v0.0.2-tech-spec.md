# Pidex v0.0.2: Project and session continuity

The implementation plan is maintained in the [GitHub milestone description](https://github.com/raeperd/pidex/milestone/2). Update that description with `gh` CLI; this file preserves existing links and is not a separate spec.
