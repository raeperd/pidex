interface:
  display_name: "Implement Issue"
  short_description: "Implement issues with behavior tests and small draft PRs"
  default_prompt: "Use $implement-issue to implement the linked issue with behavior-focused TDD and small draft PRs, using gh-stack for dependent changes."
policy:
  allow_implicit_invocation: false
