interface:
  display_name: "Pidex Create Milestone"
  short_description: "Plan milestone scope, acceptance coverage, and delivery"
  default_prompt: "Use $pidex-create-milestone to plan the agreed milestone in a concise technical spec with clear scope, ownership, acceptance scenarios, and delivery rules."
policy:
  allow_implicit_invocation: false
