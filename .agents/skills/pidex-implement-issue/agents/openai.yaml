interface:
  display_name: "Pidex Implement Issue"
  short_description: "Implement issues with TDD and reviewed, ready PRs"
  default_prompt: "Use $pidex-implement-issue to implement the linked issue with behavior-focused TDD and small PRs, using gh-stack as needed and pidex-review-pr to resolve reviews and deliver verified ready PRs."
policy:
  allow_implicit_invocation: false
