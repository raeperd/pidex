interface:
  display_name: "Create Issues"
  short_description: "Draft minimal issues with acceptance scenarios"
  default_prompt: "Use $create-issues to draft self-contained issues with acceptance scenarios and blockers, publishing only when requested."
policy:
  allow_implicit_invocation: false
